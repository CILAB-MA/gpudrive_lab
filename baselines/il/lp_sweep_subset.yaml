program: baselines/il/lp_subset.py
method: grid
name: lp_40000_subset_big_ego
metric:
  goal: minimize
  name: eval/loss
parameters:
  # MODEL
  data_path:
    values: ['processed/2000_subset']
  model_path:
    value: "/data/full_version/model/exp_40000"
  model_name:
    values: ["early_attn_s42_0828_170643"]
  # DATA
  future_step:
    values: [10, 20, 30, 40]
  num_scene:
    values: [40000]
  exp:
    values: ['ego']
  model:
    values: ['early_lp', 'final_lp', 'baseline']
  # EXPERIMENT
  batch_size:
    values: [256]
  total_gradient_steps:
    values: [200_000]
  lr:
    values: [0.0015]
  seed:
    values: [3, 11, 42]
