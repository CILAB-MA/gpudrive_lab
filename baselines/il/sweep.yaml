program: run_bc_from_scratch.py
method: grid
name: sweep
metric:
  goal: minimize
  name: eval/loss
parameters:
  # EXPERIMENT
  batch_size:
    values: [512]
  epochs:
    value: 500
  lr:
    values: [0.0003]
  sample_per_epoch:
    value: 438763

  # MODEL
  model_name:
    values: ["attention"] # ['bc', 'late_fusion', 'attention', 'wayformer']
  loss_name:
    value: "gmm" # ['l1', 'mse', 'twohot', 'nll', 'gmm', 'new_gmm']
  network_num_layers:
    values: [2]
  network_dim:
    values: [256]
  n_components:
    values: [6]
  norm:
    values: ["SBN"]
